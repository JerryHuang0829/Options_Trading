# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/

# Virtual env
.venv/
venv/
env/

# IDE
.vscode/
.idea/

# Secrets
.env
.env.*
!.env.example

# Jupyter
.ipynb_checkpoints/

# Test
.pytest_cache/
.pytest_tmp/
tests/_tmp/
.coverage

# Codex / tmp dirs at repo root (R11.19 P2 教訓: 干擾 grep / Pattern 18 doc drift sweep)
.codex_tmp/
tmp[0-9a-z]*/
tmp[0-9a-z]*

# Internal working memory + Claude Code skills — exclude from public GitHub
# (showcase 保 README.md + docs/ 技術文件；其他 .md 視為 internal working memory)
.claude/
*.md
!README.md
# Published technical docs (math derivation / audit reference / TAIFEX data spec)
!docs/bsm_derivation.md
!docs/options_math_audit.md
!docs/taifex_data_source_spec.md

# OS
.DS_Store
Thumbs.db
pytest-cache-files-*/

# TAIFEX raw cache (Phase 1 Week 3 Pre-1)
# raw ZIP / unzipped CSV — large binary, rebuild via taifex_loader.backfill_range
data/taifex_cache/raw_zip/
data/taifex_cache/raw_unzipped/

# Large raw caches (~190 MB) — rebuild via taifex_loader.backfill_range
data/taifex_cache/raw/
data/taifex_cache/strategy_view/
data/taifex_cache/_extract_tmp/
data/taifex_cache/_backfill_manifest.csv
# NOTE: surface_fits/ (15 MB, 1227 parquet) committed — dashboard SVI 3D 需要

# Historical Day 5.4 reports (superseded by week6_5yr_*)
# NOTE: week6_5yr_daily_pnl.csv (436 KB) committed — dashboard cum PnL / permutation 需要
reports/week6_5yr_no_cost/daily_pnl.csv
reports/day_5_4*

# Backtest outputs
outputs/

# Local TAIEX OHLC (regenerate via scripts/fetch_taiex.py if needed)
# data/taiex_daily.csv  ← Pre-2 完成後再決定 git track 與否（~50KB）
