# Core scientific
numpy>=1.26,<3
pandas>=2.2
scipy>=1.11

# Options pricing
py_vollib>=1.0.1

# Regime detection — src/options/regime_gate.py HMMRegimeGate (Hamilton 1989
# 2-state Markov-switching via GaussianHMM). Required by tests/options/test_regime_gate.py
# + scripts/_validate_week6_5yr.py (HMM ablation arm).
hmmlearn>=0.3,<1.0

# Testing
pytest>=8.0
pytest-cov>=5.0

# Data I/O
pyarrow>=15.0
requests>=2.31
python-dotenv>=1.0
holidays>=0.50,<1.0  # TXO 月選結算 = 第3週三+順延，holidays.TW 提供台灣假日；R11.6 P1 修法 (Codex 抓乾淨環境 import error)

# Reporting & exploration
matplotlib>=3.8
ipykernel>=6.29   # VSCode native notebook 後端；不裝 `jupyter` meta-package（Windows MAX_PATH 問題 + 用不到瀏覽器 UI）

# Dashboard (Streamlit + Plotly portfolio showcase, 本機跑於 port 8502)
streamlit>=1.31
plotly>=5.18

# Dev tooling (code quality + interview signal)
ruff>=0.6
mypy>=1.11,<2.0  # pin <2.0: mypy 2.0 + 新版 requests/holidays stubs 會把仍需要的 type:ignore 標成 unused-ignore（pyproject warn_unused_ignores=true → fail）。開發/驗證皆在 mypy 1.x。
pandas-stubs>=2.2  # 讓 mypy 對 pandas 可辨識

# Phase 2 (uncomment when integrating live trading)
# shioaji>=1.2.0
